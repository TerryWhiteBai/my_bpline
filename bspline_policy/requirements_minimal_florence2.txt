# Minimal pip requirements for training bspline_policy with Florence-2 vision encoder.
# Intended for the mpd-splines-public / CUDA 12.8 environment.
#
# Install in two steps (install PyTorch first so pip doesn't pull a CPU-only torch):
#
#   1) PyTorch
#      pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 \
#          --extra-index-url https://download.pytorch.org/whl/cu128
#
#   2) Everything else + local packages
#      pip install -r bspline_policy/requirements_minimal_florence2.txt
#

# Diffusion / vision backbone
diffusers==0.11.1
transformers==4.41.2
timm==1.0.29
huggingface-hub==0.23.4
safetensors==0.8.0
tokenizers==0.19.1

# Hydra config
hydra-core==1.3.5
omegaconf==2.3.1

# Dataset / numerical
h5py==3.16.0
zarr==2.18.4
numcodecs==0.15.1
filelock==3.25.2
numpy==2.3.1
scipy==1.17.0
numba==0.67.0
einops==0.8.2
tqdm==4.67.1
threadpoolctl==3.6.0

# Logging / image
wandb==0.21.4
pillow==12.2.0

pandas
flash_attn
# Local packages (must be installed from this repo)
-e ./robomimic
-e ./diffusion_policy
-e ./bspline_policy
